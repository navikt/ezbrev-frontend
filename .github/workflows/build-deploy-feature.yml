name: Build, deploy dev feature

on:
  push:
    branches-ignore:
      - main
      - master
      - 'dependabot/**'

jobs:
  build:
    permissions:
      contents: read
      packages: write
      id-token: write
    uses: ./.github/workflows/build-deploy-feature-frontend.yml
    secrets: inherit
